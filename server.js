const config = require('./config');const log = require('./logger').default;const RestApi = require('./backend/RestApi');var restApi = new RestApi(config);log.info('starting %s, version %s', config.NAME, config.VERSION);log.info('logLevel: %j', config.LOG_LEVEL);restApi.start();