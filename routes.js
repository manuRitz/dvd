'use strict';module.exports = function (app) {    var categoryController = require('./controllers/categoryController');    var userController = require('./controllers/userController');    var authController = require('./controllers/auth');    app.get('/', function (req, res) {        res.send('Hello World');    });    app.get('/login', authController.isAuthenticated, userController.getUser);    app.post('/login', userController.postUser);    app.get('/category', categoryController.getCategories);    app.get('/category/:id', categoryController.getCategoryById);    app.post('/category', authController.isAuthenticated, categoryController.postCategory);    app.put('/category/:id', authController.isAuthenticated, categoryController.putCategory);    app.delete('/category/:id', authController.isAuthenticated, categoryController.deleteCategory);};